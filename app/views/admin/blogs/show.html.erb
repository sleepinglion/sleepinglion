<div class="sl_show">
	<p>
		<b><%=t(:title,scope:[:activerecord,:attributes,:blog]) %></b>
		<%= @blog.title %>
	</p>
	<p>
		<b><%=t(:enable,scope:[:activerecord,:attributes,:blog]) %></b>
		<%=raw flagToString(@blog.enable) %>
	</p>
	<p>
  	<b><%=t(:label_created_at) %></b>
  	<%= @blog.created_at %>
  </p>
  <p>
  	<b><%=t(:label_updated_at) %></b>
  	<%= @blog.created_at %>
  </p>
</div>
<% if can? :update, Blog %>
<%= link_to t(:link_edit), edit_admin_blog_path(@blog) %> |
<% end %>
<%= link_to t(:link_list), admin_blogs_path %>
