<aside class="visible-lg visible-xl pull-right col-lg-3">
  <%= render 'blog_categories/aside' %>
	<% unless @tags.empty? %>
	<%= render 'blogs/tag_cloud' %>
	<% end %>
	<div class="clearfix"></div>
</aside>
